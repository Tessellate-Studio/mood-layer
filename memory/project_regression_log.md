# Regression log

Tabulated record of bugs caught in testing. Each row: symptom → root cause → fix
→ test → lesson. Read end-to-end before debugging anything that smells familiar
(bug-fix pre-flight rule in `CLAUDE.md`). 3+ rows on one theme → promote to an
anti-pattern in `project_anti_patterns.md`.

| # | Date | Symptom | Root cause | Fix | Test | Lesson |
|---|---|---|---|---|---|---|
| 1 | 2026-07-07 | Jest suite crashed on the very first render test ("Cannot read properties of undefined (reading 'constructor')" in RN's Text jest mock) | `jest-expo/android` preset + RN 0.83 + react-test-renderer 19.2 combination breaks RN's own Text mock | Switched to `jest-expo/ios` preset (verified by A/B run with identical deps) | whole suite | Preset choice is load-bearing; alate ships /ios on the same stack for the same reason |
| 2 | 2026-07-07 | Pre-device sweep: quilt weekday labels used `inkFaint` (#8A8A8A, ~3.3:1) as text — below WCAG AA | Decoration-only token used for meaningful text | `inkMuted` (7:1) + 11px in QuiltWeek `dayLabel` | sweep grep (`inkFaint` in Text style contexts) | Theme comment says "never text" — grep for it before any release |
| 3 | 2026-07-07 | Pre-device sweep: bottom Sheet wrapped its whole body in an empty-onPress Pressable → screen readers collapse the sheet into one "button", inner scroll can be swallowed | Tap-isolation wrapper copied from a modal pattern that Sheet doesn't need (sheet sits above the backdrop) | Render children directly | manual SR reasoning + suite | An empty onPress is never free — it changes the a11y tree |
| 4 | 2026-07-07 | Expo Go on Android: instant red screen on launch — "expo-notifications: Android Push notifications … removed from Expo Go with SDK 53", thrown during module init | MERELY IMPORTING expo-notifications crashes Expo Go (its init registers a push listener) — static imports in App.tsx + services/notifications | Lazy `require` behind `Constants.appOwnership === 'expo'` guard; every service fn no-ops in Expo Go; App.tsx routes through `subscribeToNotificationTaps` so the guard is the single choke point; NameItSetup shows an Expo Go caption | notifications.test.ts + device relaunch (renders) | The failure mode is at IMPORT time, not call time — guards on calls don't help; device-test early, unit mocks hide native init |
| 5 | 2026-07-08 | Device: bottom-tab labels rendered in system sans while everything else is Courier | `tabBarLabelStyle` had no `fontFamily` — react-navigation's default won | Explicit `fontFamily: fonts.body` in AppNavigator | visual (device screenshot) | Third-party chrome (nav bars, headers) doesn't inherit theme fonts — style it explicitly |
| 6 | 2026-07-10 | Device: Expo Go blue screen on launch — "Uncaught Error: java.io.IOException: Failed to download remote update / Fatal Error". Looked like the v0.2.0 redesign had broken the bundle | NOT app code. Expo Go couldn't fetch the JS bundle from the dev server: the PC's LAN IP had changed overnight (.106 → .103, DHCP) so the phone's saved `exp://…` URL was stale, and the Wi-Fi was on the **Public** firewall profile (blocks inbound 8081). `expo-updates` isn't even a dependency — "remote update" is Expo Go's own term for the dev bundle download | Reconnect Expo Go to the **current** IP (`exp://192.168.68.103:8081`); if the firewall still blocks, allow inbound TCP 8081 or `expo start --tunnel` | device reconnect renders the full app | "Failed to download remote update" / IOException in Expo Go = **dev-server connectivity** (stale IP / firewall / wrong network), never app code. Bundle compiling + jest green already rule code out — read the *actual* error text first (authoritative-source rule). The PC's LAN IP is not stable across sessions: re-read the current IP each session, don't reuse a prior day's URL |
| 7 | 2026-07-13 | Adding Circle share reminders (a 2nd local-notification scheduler) risked silently wiping the name-it reminders: name-it reschedules via `cancelAllScheduledNotificationsAsync`, and the naïve copy would have had Circle cancel-all too — each scheduler nuking the other on every settings change | Both schedulers share ONE OS notification queue; "cancel-all then reschedule" (fine when there was a single scheduler) is unsafe once a second one exists | Circle cancels only its OWN persisted ids via `cancelScheduledNotificationAsync` (per-id), never cancel-all; ids are stored per-person on `circleStore.reminderIds`. Name-it keeps its cancel-all, but Circle re-syncs on every app foreground (App.tsx `AppState 'active'`), so a rare name-it settings change self-heals Circle on next focus | notifications.test.ts (`cancelAll` NOT called; per-id cancel of both stale ids asserted) | When a second scheduler joins a shared OS queue, cancel-all is a footgun — a scheduler may cancel only the ids it owns. Re-sync on foreground as a cheap self-heal for cross-scheduler interference |
| 8 | 2026-07-17 | Device: check-in preview cluster grew odd clipped square corners at 4–5 emotions (user screenshots); on the quilt, big clusters could bleed into neighbouring patches | `clothPieces` sized pieces up to 0.94× the box AND pushed them onto a spread ring (16% offset), so intense pieces extended past the box; SVG clips at its bounds → flat edges with rounded corners poking through | Scale sizes + offsets uniformly to fit the box (`scale = min(1, halfBox / (|offset| + halfPiece))`), preserving overlap proportions | quiltLayout.test.ts containment regression (k=1..5, intensity 4, two box shapes) | Layout math that composes two "safe" maxima (piece size, ring radius) can exceed the container only in combination — test the composed extremes, not each factor alone |
| 9 | 2026-07-18 | Detail sheet on the home quilt would not scroll — a check-in with a dozen emotions showed its top but the bottom was unreachable (reported THREE times, each "fixed" by adjusting flex sizing) | The ScrollView sat inside TWO nested `Pressable`s (backdrop → sheet). On Android a Pressable ancestor claims the pan/move gesture, so a nested ScrollView never scrolls — a sizing problem it never was | Backdrop is now a plain `View` with an `absoluteFill` Pressable SIBLING behind the sheet; the sheet is a plain `View` with `onStartShouldSetResponder` to swallow taps. No Pressable ancestry wraps the scroll area | manual device scroll + patch-detail render test | When a ScrollView "won't scroll" on Android, look UP the tree for a Pressable/Touchable ancestor before touching flex — gesture-claiming ancestry is the usual cause, and flex tweaks paper over it (3 rounds lost here) |
| 10 | 2026-07-18 | After merging master's "ignore .claude/worktrees in jest discovery" commit, `npx jest` in a worktree session discovered ZERO tests and reported green — a suite that tested nothing | `testPathIgnorePatterns: ['/\.claude/worktrees/']` is a bare substring, so it also matches the path of a worktree running its OWN suite (every test file's path contains `.claude/worktrees/…`) | Anchor the ignore to `<rootDir>`: `'<rootDir>/\.claude/worktrees/'`, which only matches worktrees nested BELOW the current root | `jest --listTests` count (0 → 33) | A path-ignore meant for "nested copies" must be `<rootDir>`-anchored; a bare substring silently swallows the host suite when the host itself lives at that path. "Green" with a suspiciously fast run = check `--listTests` count |
| 11 | 2026-08-13 | `npx jest` failed intermittently on QuiltScreen — 3 tests one run, 1 the next, 0 when the file ran alone (10/10). Every failure read `Exceeded timeout of 5000 ms`, never an assertion. Surfaced while adding crash reporting, so it first looked like the new import chain (`settingsStore` → `crashReporting`) had slowed a screen the test file imports | NOT the change. Verified by running the full suite on an UNMODIFIED `origin/master`: 3 failures there too, same file, same timeout. Jest's 5 s default is simply too tight for this suite's heaviest RTL render once 36 suites contend for workers — the file needs ~53 s on its own | `testTimeout: 30000` in `jest.config.js`, with the reasoning inline | the suite itself (flake, so absence of failure is the signal) | Before blaming your diff for a flake, run the suite at the merge-base — the answer costs one run and the alternative is redesigning around a ghost. And a gate that fails for reasons unrelated to the diff trains people to re-run instead of read, which is how a real failure gets waved through |
| 12 | 2026-08-13 | The brand-new pre-push hook rejected every push with a single line — `failed to push some refs` — and **no other output at all**. Run directly it printed nothing and exited non-zero, before even its first `echo` | `set -e` plus `WT_HOOKSPATH=$(git config --worktree core.hooksPath)`. `git config` exits **1** when the key is unset (the normal case), and under `set -e` an assignment from a failing command substitution terminates the script immediately — so the hook died at line one, silently, and git reported only the refusal | `|| true` on that substitution, with the reason inline | the hook's own run: `sh .husky/pre-push` now reaches `✅ pre-push checks passed` (339/339, ~95 s) | Two lessons. (1) `VAR=$(cmd)` under `set -e` is a hidden exit whenever cmd can legitimately fail — `git config --get`, `grep`, `command -v` all do. (2) A gate that blocks with no message is worse than no gate: run any new hook DIRECTLY before trusting it, because git swallows hook stdout on failure and you will blame the ref, the remote, or your credentials first |
| 13 | 2026-08-13 | Adding one dependency (`@sentry/react-native`) made Linux CI's `npm ci` fail: `Missing: @emnapi/core@1.11.3 from lock file`. Neither a Windows `npm install`, nor `--package-lock-only`, nor the repo's own `relock.yml` running `npm install` on ubuntu put the entries back | Installing on Windows PRUNED two top-level **optional, wasm-platform** entries (`@emnapi/core`, `@emnapi/runtime` @1.11.3) that master's lockfile carried — they belong to `@unrs/resolver-binding-wasm32-wasi`, needed by no real platform, so every `npm install` on a concrete OS considers them irrelevant. `npm ci` disagrees: it validates the WHOLE graph, including branches it will not install. Dependabot's lockfiles include them, which is why master had them and no earlier branch tripped this | Copied the two entries verbatim from `origin/master`'s lockfile into the branch's, then re-sorted `packages` (a 35-line diff, all metadata) | `npm ci` on Linux — the `inspect` job, which is the only check that runs it | Adding a dep on Windows can DELETE lockfile entries unrelated to that dep. After any Windows install, diff `package-lock.json` against master for *removals* (`git diff master -- package-lock.json | grep '^-'`) before pushing — and do not expect a Linux re-lock to undo it: `npm install` prunes the same branches on every platform; only `npm ci` reads them |
| 14 | 2026-08-13 | The pre-push hook shipped that morning was itself corrupting `package-lock.json`: every push where the lockfile looked stale ran `npm install`, which pruned the optional wasm entries of row 13 — and the mutation then rode along in whatever commit came next. Caught when a docs-only PR turned up carrying a 35-line lockfile deletion nobody wrote | The hook chose `npm install` over `npm ci` to dodge the Windows tree-wipe ENOTEMPTY race. That trade was wrong in one direction it did not consider: ENOTEMPTY is loud, immediate and retryable; a pruned lockfile is silent, survives review, and breaks `npm ci` for everyone downstream | Hook now runs `npm ci` (retried once), and **hard-fails the push** if `package-lock.json` is dirty afterwards — a gate may not mutate tracked files | the hook's own guard: `git diff --quiet -- package-lock.json` after the install step | When choosing between two failure modes, prefer the loud one. "Avoid the noisy error" is how you end up shipping the quiet one — and tooling that edits tracked files as a side effect will eventually get those edits committed by someone who wasn't looking |
| 15 | 2026-08-16 | `insightEngine.test.ts › counts distinct active days` failed for the first time — `checkInCount` 2, expected 3 — on a branch whose diff was two CI/docs files. The same suite had passed 339/339 the day before, and CI passed on the exact commit being pushed | The fixtures were `now - 7` and `now - 6`, asserted to share an ISO week. `weekKey` ([`src/utils/dates.ts:27`](../src/utils/dates.ts)) starts weeks on Monday, so those two days straddle a week boundary whenever `now - 7` is a Sunday. 2026-08-16 was a Sunday; `now - 6` fell into the next week and was filtered out. Not the diff, not the environment — **the calendar** | Anchor the fixtures to the MONDAY of the target week (`inWeek - ((getDay()+6)%7)`) and use Monday + Tuesday, which share an ISO week on every date in every timezone | the test itself; plus a 365-day sweep of both fixture schemes against `weekKey` — old leaves the week on 52 days/year, new on 0 | A `now`-relative fixture spanning a day boundary is a time bomb with a known fuse length: it fails on the 1-in-7 (or 1-in-365) days the boundary lands wrong, usually weeks after it was written, on someone else's unrelated branch. Anchor relative dates to the boundary they care about (start of week/month), and when a test fails on a diff that cannot explain it, check the date before the diff — row 11's "run the merge-base first" applies, but the merge-base passes on the wrong day too |
| 15 | 2026-08-16 | The first Android build after adding crash reporting FAILED — both on EAS (`EAS_BUILD_UNKNOWN_GRADLE_ERROR`) and in the CI APK workflow. 339 unit tests, `tsc`, eslint and three CI checks had all been green; nothing caught it, because nothing had built a binary since the dependency landed | `@sentry/react-native`'s `sentry.gradle` (line 132) registers a `…_SentryUpload_…` task that shells out to `sentry-cli` to upload source maps. With no `SENTRY_AUTH_TOKEN`/org/project it exits 1, and Gradle fails the whole assemble. The config plugin had warned about the missing org/project at `eas init` time and it was read as cosmetic — it was the defect announcing itself | `SENTRY_DISABLE_AUTO_UPLOAD=true` in all three `eas.json` build profiles AND in the CI workflow's build step. Source-map upload is deliberately declined: it needs a build-time credential + network on every build, to buy symbolication we can live without. Crash reporting itself is unaffected; traces arrive minified | The build itself — the only thing that can verify this class. Re-ran both the CI APK build and the EAS build after the fix | **A native dependency is not "shipped" until a binary is built.** `CLAUDE.md` already says Expo Go is not proof the binary works (regression #4, the v1 launch); this is the same lesson at build time rather than run time. Unit tests mock native modules, so they cannot see a Gradle task, a config plugin, or a missing credential. When adding ANY dependency with native code or a config plugin, dispatch `build-android-apk.yml` in the same session — before calling it done |
| 16 | 2026-08-17 | The crash-reporting privacy contract — the thing the whole feature was approved on — did **not** hold. A forced native crash on a real Pixel 2 XL reached Sentry (MOOD-LAYER-1) carrying `user.id` and `user.geo` (**city-level location**, Bengaluru), plus the device install id. `docs/SECURITY.md`, ADR-001 and `CLAUDE.md` all stated those fields were stripped | A NATIVE crash is captured and transmitted by the Android SDK's `UncaughtExceptionHandlerIntegration` **without ever entering the JS layer**, so `beforeSend` → `scrubEvent` never runs. The 8 scrubbing tests all passed throughout, because every one of them constructs a JS event object and calls `scrubEvent` directly — they proved the scrubber works, never that the scrubber is reached. Two independent leaks in one event: SDK-attached `user.id`, and `user.geo` which Sentry derives SERVER-SIDE from the request IP regardless of `sendDefaultPii: false` | `enableNativeCrashHandling: false` — every event Sentry can now receive is a JS event, so the documented contract is true by construction. Accepted cost: native-only failures (OOM/ANR/native-module) go unreported. Server-side half needs a human: enable "Prevent Storing of IP Addresses" on the project | `src/__tests__/crashReporting.test.ts` now asserts the **init options** (`enableNativeCrashHandling === false`, `beforeSend` wired, `sendDefaultPii === false`), not just the scrubber's behaviour — the gap was never in the function, it was in which events reach it | A test that verifies a transform will happily pass while nothing routes data through it. Test the WIRING, not only the function: for any privacy or security control, assert the config that makes it reachable. And a contract about what leaves a device is not verified until an event is inspected AFTER it arrives — the local suite cannot see the wire. This one only surfaced because the user asked for an end-to-end device test |
| 17 | 2026-08-18 | End-to-end device test of the JS crash path (MOOD-LAYER-2) showed the scrubber working — no `user`, no `extra`, no state, no console/network breadcrumbs — but the event STILL carried two identifiers: `contexts.device.id`, a stable per-install uuid, and `user.geo` (city-level: Bengaluru) | Two separate causes. (1) `scrubEvent` deleted `event.user`, but the SAME uuid that had been `user.id` on the native crash is also emitted as `contexts.device.id` — removing one field just relocated the identifier. (2) `user.geo` is derived SERVER-SIDE at ingest from the request IP; Sentry's "Prevent Storing of IP Addresses" drops the raw IP but not the geo computed from it, so no SDK-side change can remove it | Code half: `delete event.contexts.device.id` in `scrubEvent`, keeping model/battery/memory (diagnostic) — pinned by a test using the real uuid from the event. Server half: needs an Advanced Data Scrubbing rule on the Sentry project (`[Remove] [Anything] from [user.geo]`) — configuration, not code | `src/__tests__/crashReporting.test.ts` asserts device.id is gone and model survives; the definitive check remains reading a real arrived event | An identifier removed from one field can be present in another — scrub by asking "what in this payload names the device or the person", not by ticking a list of field names. And a privacy control that lives half in code and half in vendor settings is only as strong as the half nobody tested: the code half passed 8 tests for two days while the vendor half shipped a city name |
| 18 | 2026-08-19 | Three separate attempts to stop city-level geolocation reaching Sentry all failed, each confirmed against a real delivered event rather than assumed: `sendDefaultPii: false`, "Prevent Storing of IP Addresses" (org + project), and an Advanced Data Scrubbing rule on `$user.geo` | `user.geo` is produced by Sentry's INGEST pipeline from the request IP. Data-scrubbing rules run against the event as the SDK submitted it, where `user.geo` does not yet exist — so the rule matches nothing, and disabling IP *storage* removes the raw IP while keeping the geography already derived from it. No SDK option or project setting sits upstream of that derivation | None in code. Recorded as an **accepted residual** in `docs/SECURITY.md` with the three failed attempts and their evidence, so the next person does not spend the same afternoon rediscovering it. Remaining routes if it ever matters: Sentry support, or self-hosted Relay ahead of ingest | Not testable locally by construction — the only check is reading a delivered event, which is how each failure was found | Some privacy controls live upstream of every knob you own. When a vendor derives a field server-side, SDK config and scrubbing rules are both downstream of it and cannot win — recognise that shape early and switch from fixing to documenting. Three rounds were spent proving it here; the record exists so it is one lookup next time |
| 19 | 2026-08-20 | The Android **App permissions** screen for a text-and-colour journal listed **Camera, Files and media, Microphone** — three sensitive permissions for an app that has one camera feature and no audio or file features at all. Shipped that way in 0.2.0 | Two of the three were merged in from dependency manifests, which the build never surfaces. `RECORD_AUDIO` comes from [`expo-camera`'s own AndroidManifest](../node_modules/expo-camera/android/src/main/AndroidManifest.xml) (its plugin adds it for video capture, `recordAudioAndroid` defaults **true**) and was ALSO hardcoded in `app.json`'s `android.permissions`; `READ/WRITE_EXTERNAL_STORAGE` come from `expo-file-system`, which declares them for shared-storage access this app never makes — it writes only inside its own sandbox. Camera is genuine: [`PairSheet.tsx:166`](../src/components/PairSheet.tsx#L166) scans the circle-invite QR | `recordAudioAndroid: false` on the expo-camera plugin (stops the merge at source) plus `android.blockedPermissions` for all three (catches any dependency that re-adds one). Verified in the resolved manifest: the three carry `tools:node="remove"`, CAMERA does not | `src/__tests__/androidPermissions.test.ts` — pins the surface rather than the config, so a future `expo install` that re-adds one fails in CI instead of on a user's permissions screen | **A dependency can widen your permission surface and nothing fails.** No test, typecheck or lint sees the merged manifest; the first honest reading is the App permissions screen on a real device, which is also where a user's trust is decided. Read that screen after adding any native dependency, and treat every entry as something you must be able to justify out loud — `adb shell dumpsys package <id>` gives the same list without waiting for a store listing |
| 20 | 2026-08-20 | The runbook told the user to grant a notification permission before two device tests could run. There was nothing to grant: the App permissions screen has no notifications entry at all, and the two tests had been sitting blocked on a prerequisite that does not exist on that phone | `POST_NOTIFICATIONS` is an Android **13+** (API 33) runtime permission. The Pixel 2 XL runs Android 11 (SDK 30), where notifications are enabled by default and live under App info → Notifications, not App permissions. The permission IS in the merged manifest — `expo-notifications` declares it for newer devices — and that listing was read as an outstanding grant without checking the device's API level | Runbook corrected with the two commands that settle it: `dumpsys package` shows `POST_NOTIFICATIONS` under *requested* but not *runtime permissions*, and `cmd appops … POST_NOTIFICATION` reports `Default mode: allow` | None — this is a documentation defect, not a code one. The check is the two adb commands, now written into the runbook next to the claim | A manifest entry describes what the app would ask for on **some** device, not what is outstanding on **this** one. Android gates permissions on API level, so any claim of the form "grant X first" has to be read against `ro.build.version.sdk` before it goes in a runbook — otherwise it parks real work behind an imaginary blocker, which is what happened here for a day. Same shape as rows 16–18: a statement about the device is only true once the device has been asked |
| 21 | 2026-08-20 | The Name-it reminder's whole point is to bring you back into the app, and it did not. The notification fires correctly with the app closed, but tapping it opens **Layers**, not the check-in flow. No crash, no log line, nothing in logcat — it just quietly lands on the wrong screen | [`navigate()`](../src/navigation/navigationRef.ts) returns early when `navigationRef.isReady()` is false, and [`App.tsx:84`](../App.tsx) gates the entire tree — navigator included — on `useFonts`. The tap subscription is installed on the FIRST render, and `getLastNotificationResponseAsync()` resolves in milliseconds while fonts take far longer, so the deep link is handed to a navigator that does not exist yet and is dropped. Every notification tap is a cold start by definition: the process only existed because the alarm woke it to post the notification | None yet — filed as [#73](https://github.com/Tessellate-Studio/mood-layer/issues/73) with the fix shape (hold the pending route in `navigationRef`, flush it from `NavigationContainer`'s `onReady`). The Circle branch at `App.tsx:52` has identical exposure | The discriminating device test, not the suite: same notification, same payload, same handler — tapped cold it lands on Layers, tapped with the app already open it lands on "Can you name it?". Readiness is the only variable | A guard that turns a crash into a silent no-op moves the failure from a stack trace to a screen nobody is watching. `if (!ready) return` is a decision to lose the event — if the event matters, queue it and flush on ready. And note the shape of the exposure: this feature works in precisely the case where it is not needed (app already open) and fails in the case it exists for. Any "is it ready yet" guard on a user-initiated route deserves that question asked out loud |
| 22 | 2026-08-20 | The circle "A week from X arrived" notification — documented in its own function as "one **quiet** IMMEDIATE local notification" — arrives as a heads-up at HIGH importance, and the `Circle share reminders` channel a user would go to Android settings to mute has nothing delivered on it | [`notifyCircleReceived`](../src/services/notifications.ts#L138) calls `ensureCircleChannel()`, so the `circle` channel is created correctly (DEFAULT importance, `sound: null`) — and then never attached, because on Android the channel travels on the **trigger** and this call passes `trigger: null`. expo-notifications falls back to its own channel, which it creates at HIGH importance. `scheduleNameItReminders` passes `channelId` inside its DAILY trigger, which is why name-it lands on `channel=name-it` at importance 3 and this one lands on `expo_notifications_fallback_notification_channel` at importance 4 | None yet — filed as [#74](https://github.com/Tessellate-Studio/mood-layer/issues/74). `ChannelAwareTriggerInput` is exactly this case: `trigger: { channelId: CIRCLE_CHANNEL_ID }`, documented as delivered immediately | Reading the arrived notification off the device with `dumpsys notification --noredact` — both notifications in one dump, side by side | Creating a channel and delivering on it are two different things, and only the second is observable. The tests mock expo-notifications, so `channelId` is recorded as an argument and never resolved against a real NotificationManager — the same gap as row 16, where a scrubber passed eight tests while nothing routed events through it. When a config object exists to shape how something reaches a user, assert the delivered artifact, not the call |
